import './index.css';

interface SelectorProps {

}

const Selector = (props:SelectorProps) => {
    return (
        <div >
            Selector component
        </div>
    )
};

export default Selector;